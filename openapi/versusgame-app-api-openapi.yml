openapi: 3.2.0
info:
  title: Versusgame App API
  description: APIs for Versusgame Application
  version: '1.0'
  contact: {}
tags:
- name: App
paths:
  /:
    get:
      operationId: AppController_greeting
      parameters: []
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                type: string
      tags:
      - App
components:
  securitySchemes:
    access-token:
      scheme: bearer
      bearerFormat: JWT
      description: Enter the bearer token below (do not include 'Bearer')
      type: http
    x-api-key:
      type: apiKey
      in: header
      name: x-api-key
      description: API Key For External calls
